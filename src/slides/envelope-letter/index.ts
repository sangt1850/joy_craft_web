import { registerSlide } from "../registry";
import EnvelopeLetter from "./EnvelopeLetter";
export { EnvelopeLetter };
export { schema, defaultValues } from "./schema";
registerSlide("envelope-letter", EnvelopeLetter);
